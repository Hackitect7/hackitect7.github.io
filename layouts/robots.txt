{{- /* layouts/robots.txt */ -}}
{{- $noindex := or
  (ne hugo.Environment "production")  /* всё, что не production */
  .Site.Params.noindex                /* глобально запретить индексацию */
  .Params.noindex                     /* запретить для конкретной страницы/раздела */
-}}

User-agent: *
{{- if $noindex -}}
Disallow: /
{{- else -}}
Allow: /

# Системные разделы, которые не хотим индексировать
Disallow: /tags/
Disallow: /categories/
Disallow: /drafts/

# Карта сайта
Sitemap: {{ "sitemap.xml" | absURL }}
{{- end -}}
